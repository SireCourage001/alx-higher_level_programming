#!/usr/bin/node
const argum = process.argv[2];
if (isNaN(argum)) {
	console.log("Missing size");
} else {
  for (let i = 0; idx < parseInt(argum); idx++) {
	console.log('X'.repeat(parseInt(argum)));
}

